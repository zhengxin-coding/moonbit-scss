import {
  "localreview/scss" @lib,
  "moonbitlang/core/json",
}

supported_targets = "js"

pkgtype(kind: "executable")

options(
  link: { "js": { "exports": [ "run", "project" ], "format": "esm" } },
)
