# 常用选择器指令 · 0.6

新增 `@extend`、`!optional`、占位选择器及 `@at-root`。CSS 规则延后输出，使后声明的继承、传递继承和模块内的规则能够共同处理。

- `@extend`：类、ID、类型、属性和伪类等简单目标；选择器列表、常用复合选择器合并、简单后代路径、传递继承、未使用占位规则移除。缺失非 optional 目标报错；媒体查询内不能扩展不同媒体上下文的规则。
- 模块：扩展只作用于本模块和其依赖；支持间接依赖和 forward，兄弟模块隔离，私有占位符保持模块隔离。
- `@at-root`：选择器形式、块形式、`with`/`without` 查询；规则/媒体/supports/layer 上下文筛选，保留显式父选择器及常见 `#{&}` 插值。移出容器的规则在该容器之后输出。

固定 Dart Sass 1.104.0 的 70 项新增独立对照通过；成功 CSS 经官方 CSS 编译器压缩后精确比较，错误只比较拒绝。预期值由官方编译器生成，加入 JS/Wasm-GC 黄金测试。原有 808 项对照与原有主机/CLI/限制检查仍通过。

范围限制：不是完整的 Sass 选择器代数。多层复杂路径的所有交织/去冗余、函数伪类内部扩展、命名空间与 CSS 转义等角落尚未全面覆盖；部分复杂 extender 组合明确报错。`&` 作为任意 SassScript 值仍未实现。每条规则最多展开 256 个选择器、项目最多 4096 项扩展，并受共享求值与输出限额约束。

原始结果见 evidence/selector-comparison.json；当前源码指纹以 evidence/selector-upgrade.json 为准。旧合集保持历史快照，后续交付需绑定本轮提交。
