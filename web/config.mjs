export default {"slug": "scss", "title": "SCSS 子集编译器", "scope": "变量、嵌套选择器、父选择器与作用域", "source": "https://sass-lang.com/documentation/style-rules/", "limitations": "支持简单选择器、嵌套、&、词法变量和 CSS 声明；不含 @use/@import、mixin、函数、运算、插值、媒体规则；含 [] 或 () 的复杂选择器明确拒绝，不是 dart-sass 全兼容实现。", "recommendation": "保留候选：先补边界和上游兼容范围，再决定是否申报。", "version": "0.1.0", "example": "$accent: #4ca98f;\n.card, .panel {\n  color: $accent;\n  padding: 12px;\n  &:hover { color: white; }\n  .title { font-weight: bold; }\n}"};
