export default {"slug": "scss", "title": "SCSS 子集编译器", "scope": "变量、嵌套选择器、父选择器与作用域；增加保留父选择器上下文的嵌套 @media 编译", "source": "https://sass-lang.com/documentation/style-rules/", "limitations": "无 mixin、函数、模块、运算、插值和完整复杂选择器；不是 Sass 全兼容", "recommendation": "保留候选：先补边界和上游兼容范围，再决定是否申报。", "version": "0.2.0", "example": "$accent: #4ca98f;\n.card, .panel {\n  color: $accent;\n  padding: 12px;\n  &:hover { color: white; }\n  .title { font-weight: bold; }\n}\n.card { @media screen { color: blue; &:hover { color: red; } } }"};
