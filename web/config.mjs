export default {"slug": "scss", "title": "SCSS 子集编译器", "scope": "词法变量与 !default/!global、嵌套选择器、独立父选择器组合、伪类和属性选择器、mixin 默认/关键字/可变参数、调用方作用域的 @content、变量/字符串插值、嵌套属性、media/supports/layer/font-face。", "source": "https://sass-lang.com/documentation/style-rules/", "limitations": "尚未实现模块和文件导入、完整表达式与算术、内置/用户函数、流程控制、@extend、完整 at-rule 与选择器语义、注释及自定义属性空白的完整保留；不是 Dart Sass 全兼容实现。", "recommendation": "保留候选：先补边界和上游兼容范围，再决定是否申报。", "version": "0.3.0", "example": "$accent: #4ca98f;\n.card, .panel {\n  color: $accent;\n  padding: 12px;\n  &:hover { color: white; }\n  .title { font-weight: bold; }\n}\n.card { @media screen { color: blue; &:hover { color: red; } } }"};
