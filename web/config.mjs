export default {"slug": "scss", "title": "SCSS 工作台", "scope": "类型化值、单位算术、用户函数与流程控制，SCSS 文件模块及共享作用域。", "source": "https://sass-lang.com/documentation/style-rules/", "limitations": "已实现数值、字符串、列表和映射、用户函数、if/for/each/while、math/list/map/string/meta 的常用函数，以及相对 SCSS 模块 use/forward、配置和命名空间。尚缺完整颜色与 calc 语义、全部内置函数、标准模块 as * / forward、旧式 import、extend、源码映射和完整 Sass 语法。", "recommendation": "保留候选：先补边界和上游兼容范围，再决定是否申报。", "version": "0.4.0", "example": "$accent: #4ca98f;\n.card, .panel {\n  color: $accent;\n  padding: 12px;\n  &:hover { color: white; }\n  .title { font-weight: bold; }\n}\n.card { @media screen { color: blue; &:hover { color: red; } } }"};
